# 本地 RAG 项目质量评测方案

## 1. 评测目标

本文用于在项目交付前，从以下四个维度客观评估本地 RAG 系统的完成质量：

1. 知识库质量：原始资料是否被完整、正确地解析、切分和索引。
2. 路由质量：系统是否能正确判断问题需要查询本地知识库还是直接回答。
3. 检索质量：需要使用知识库时，系统能否将正确证据排在靠前位置，并最终送入模型上下文。
4. 答案质量：最终回答是否正确、完整、忠于证据，并能在资料不足时安全拒答。

评测必须做到：指标定义明确、测试数据固定、过程可复现、结果可追溯。所有正式指标均使用冻结后的测试集计算；开发调参只能使用开发集，不能根据正式测试集结果反复修改参数。

## 2. 当前系统与评测边界

本项目的完整链路为：

```text
原始 Word/法规资料
  -> 文档扫描与解析
  -> 文本清洗和法规层级识别
  -> Chunk 切分
  -> Embedding 与 FAISS 索引
  -> SQLite FTS 全文索引
  -> RAG 路由
  -> 向量 + FTS 混合检索及 RRF 融合
  -> 上下文预算筛选 selected_hits
  -> 基于证据生成答案
  -> 追加参考文件和位置
```

本方案只评估上述链路的业务质量，不将接口吞吐量、硬件资源利用率、安装便利性等性能或工程指标计入四个质量总分。自动化单元测试通过率可作为评测开始前的准入条件，但不能代替业务质量评测。

## 3. 评测基本原则

### 3.1 冻结评测环境

每次正式评测前记录并冻结以下信息：

- 项目 Git 提交号或交付版本号；
- 原始资料目录的文件清单和文件 SHA-256；
- `metadata.db`、`index.faiss`、Embedding 元数据文件的 SHA-256；
- LLM、Embedding 模型名称及版本；
- 路由模型和生成模型参数；
- `RAG_VECTOR_TOP_K`、`RAG_FTS_TOP_K`、`RAG_FINAL_TOP_K`、RRF 权重；
- 上下文窗口和各项 token 预算；
- 测试机器、操作系统和评测时间；
- 评测集版本号。

正式测试期间不得更换模型、知识库或检索参数。如果发生变更，必须生成新的 `run_id`，重新执行完整评测。

### 3.2 评测数据隔离

建议将题目分为：

- 开发集：约 30%，用于调试路由提示词、Top-K 和 RRF 权重；
- 测试集：约 70%，仅用于正式验收和交付报告；
- 回归集：从已发现的历史缺陷中持续沉淀，每次发布必须全部通过。

同一法规或同一问题的简单改写不得跨开发集和测试集分布，以免发生数据泄漏。

### 3.3 统计口径

- 除特别说明外，所有比例指标均同时报告“分子/分母”和百分比。
- 对检索与答案指标同时报告总体结果和分场景结果，不能只展示总体平均值。
- 正式答案评测建议每道题重复生成 3 次，报告平均值及最差一次结果；路由温度为 0 时仍建议检查重复一致性。
- 严重错误单独计数，不允许被平均分掩盖。严重错误包括：编造法规条文、给出与证据相反的结论、泄露绝对路径或私有信息、应拒答却给出确定性答案。

## 4. 黄金评测集设计

### 4.1 题目规模与构成

建议首版准备 240 道正式题目，最低不应少于 150 道。推荐构成如下：

| 类别 | 建议数量 | 预期路由 | 用途 |
| --- | ---: | --- | --- |
| 单文件、单条款可回答 | 50 | RAG | 测试精确事实和定位能力 |
| 单文件、多条款综合 | 35 | RAG | 测试多证据召回和答案完整性 |
| 跨文件综合 | 30 | RAG | 测试跨文档检索和综合回答 |
| 语义改写、无原文关键词 | 25 | RAG | 测试向量语义召回 |
| 精确名称、编号、专有词 | 20 | RAG | 测试 FTS 和精确匹配能力 |
| 知识库无答案 | 40 | RAG | 测试检索噪声和安全拒答 |
| 通用知识、写作、翻译、闲聊 | 25 | Direct | 测试是否错误调用知识库 |
| 指代追问、多轮对话 | 10 | 按上下文标注 | 测试路由和答案对历史信息的使用 |
| 模糊问题、提示词注入 | 5 | RAG/安全兜底 | 测试保守路由和抗干扰能力 |
| 合计 | 240 | — | — |

题目还应附加法规类别、文件来源、难度、是否多跳、是否含否定、是否含时间条件等标签，以便生成分组报告。

### 4.2 每道题的标注字段

建议使用 JSONL 保存黄金题目，每行至少包含：

```json
{
  "case_id": "rag_0001",
  "question": "测试问题",
  "history": [],
  "category": "single_article",
  "difficulty": "medium",
  "expected_need_rag": true,
  "expected_reason_code": "private_knowledge",
  "answerable": true,
  "gold_documents": ["相对路径或稳定 doc_id"],
  "gold_articles": ["第三条"],
  "required_evidence": ["chunk_id 或证据组 ID"],
  "acceptable_evidence": ["其他同样能够支持答案的 chunk_id"],
  "required_key_points": ["必须答出的事实 1", "必须答出的事实 2"],
  "forbidden_claims": ["常见错误结论"],
  "reference_answer": "仅供评审使用的参考答案",
  "annotator": "A",
  "reviewer": "B",
  "dataset_version": "v1.0"
}
```

`required_evidence` 应允许用“证据组”表示，例如某个问题必须同时命中 A 条款和 B 条款，但每个条款可能对应多个内容等价的 Chunk。这样可以避免把切分位置的偶然差异误判为检索错误。

### 4.3 标注流程

1. 标注人员从原始法规或客户资料中设计问题，不直接从 Chunk 文本机械复制问题。
2. 标注预期路由和是否可回答。
3. 对可回答问题标注正确文件、条款、必需证据、可接受证据和关键事实。
4. 对无答案问题明确记录“知识库范围内不可回答”，并说明相邻但不足以回答的干扰资料。
5. 第二名评审人员独立复核。
6. 有争议的样本由业务专家裁决；未达成一致的题目不得进入正式测试集。

建议统计标注一致率：

```text
标注一致率 = 初次无需仲裁且结论一致的样本数 / 双人标注样本总数
```

路由标签可补充 Cohen's Kappa；建议 Kappa 不低于 0.80。低于该值通常表示题目定义或标注规则不够清楚，应先修订评测集。

## 5. 知识库质量评测

知识库质量分为自动一致性检查和人工内容抽检。自动检查覆盖全部数据，人工检查采用分层随机抽样。

### 5.1 指标定义

| 指标 | 计算方法 | 说明 | 建议门槛 |
| --- | --- | --- | ---: |
| 文件扫描覆盖率 | 已登记的合格源文件数 / 原始目录合格文件数 | 检查漏扫文件 | 100% |
| 文档解析成功率 | `parse_status=success` 文档数 / 应解析文档数 | `failed` 和 `empty` 不计成功 | ≥99.5%，关键文件 100% |
| 非空内容率 | 产生有效正文块的文档数 / 解析成功文档数 | 防止“状态成功但正文为空” | 100% |
| 元数据完整率 | 必填元数据均有效的文档或 Chunk 数 / 总数 | 至少检查 `doc_id`、标题、相对路径、Chunk ID | 100% |
| 法规结构识别准确率 | 人工判断正确的层级/条款字段数 / 抽检字段总数 | 检查章、节、条和 `section_path` | ≥98% |
| Chunk 边界合格率 | 语义完整且未错误跨条款的 Chunk 数 / 抽检 Chunk 数 | 允许设计要求内的重叠 | ≥98% |
| Chunk 内容忠实率 | 与源文档一致且无乱码、丢字、错序的 Chunk 数 / 抽检 Chunk 数 | 内容错误属于高风险问题 | ≥99% |
| Chunk 长度合规率 | token 数处于配置允许范围的 Chunk 数 / 总 Chunk 数 | 对特殊超短标题块单独说明 | 100% |
| FTS 覆盖一致率 | FTS 中可检索 Chunk 数 / 数据库有效 Chunk 数 | 检查全文索引漏项 | 100% |
| Embedding 成功率 | `embedding_status=success` Chunk 数 / 有效 Chunk 总数 | 检查向量化失败 | 100% |
| 向量索引一致率 | FAISS 向量数、成功 Embedding 数、唯一 vector_id 数的一致程度 | 三者必须相等且 ID 映射正确 | 100% |
| 重复与孤儿记录率 | 重复 ID、无父文档 Chunk、无 Chunk 向量等异常记录数 / 对应总数 | 越低越好 | 0% |

### 5.2 自动一致性测试

对 `Rawdata`、`metadata.db`、FTS 和 FAISS 执行全量检查，至少验证：

1. 原始目录中的合格 Word 文件都存在于 `source_files`。
2. 每个应解析文件都有唯一 `documents` 记录。
3. `parse_status` 的成功、失败、空文档数量与构建日志一致。
4. 成功文档至少存在一个有效 `parsed_blocks`、`structured_blocks` 和 `chunks` 记录。
5. 每个 Chunk 的 `doc_id` 均能关联有效文档，`chunk_id` 唯一。
6. `chunk_text` 和 `chunk_text_for_embedding` 非空。
7. `section_path`、`article_no`、`relative_path` 等字段格式合法；路径必须是相对路径。
8. FTS 记录数与有效 Chunk 数一致，并随机选择精确词验证可检索。
9. Embedding 成功数、向量文件有效记录数和 FAISS `ntotal` 一致。
10. Embedding 模型名和维度与运行时配置一致。
11. `vector_id` 唯一、连续性符合构建规则，并且 FAISS 命中能反查到同一 Chunk。
12. 数据库中不存在孤儿记录、重复 Chunk、重复向量或未处理构建错误。

现有工具可以作为自动检查入口：

```powershell
python .\tools\check_embedding_consistency.py --mode full --require-complete
python .\tools\inspect_db.py
```

正式评测工具还应将上述检查结果写入结构化 JSON，而不应只依赖终端中的 PASS/FAIL 文本。

### 5.3 人工抽检

人工抽检必须按资料来源、法规类别、文件长度、是否含表格、是否有复杂层级进行分层，不能只检查前若干条记录。

- 文档级抽样：建议至少 200 份；若总文档不足 200 份则全检。
- Chunk 级抽样：总体较大时建议抽检 384 条，可近似获得 95% 置信水平、±5% 误差范围的保守估计。
- 高风险资料：包含表格、废止/修订状态、密集数字、复杂条款层级的文件额外抽样，不计入纯随机样本。

每条抽检记录至少填写：源文档是否匹配、文本是否完整、条款号是否正确、层级路径是否正确、Chunk 边界是否合理、是否包含噪声、是否泄露绝对路径。

### 5.4 知识库质量判定

以下任一情况建议判定知识库维度不通过：

- 关键客户文件漏入库；
- FAISS、Embedding 和数据库映射不一致；
- 出现无法追溯到源文件的 Chunk；
- 抽检发现系统性乱码、丢段或条款错位；
- 绝对路径或不应展示的本地信息进入可返回元数据。

## 6. 路由质量评测

本项目路由输出包含 `need_rag` 和 `reason_code`。正式评测以 `need_rag` 的二分类结果为主，`reason_code` 为辅助诊断指标。

将“需要查询知识库”定义为正类，将“可以直接回答”定义为负类：

- TP：应使用 RAG，实际使用 RAG；
- FN：应使用 RAG，实际直接回答；
- FP：应直接回答，实际使用 RAG；
- TN：应直接回答，实际直接回答。

### 6.1 核心指标

| 指标 | 公式 | 业务含义 | 建议门槛 |
| --- | --- | --- | ---: |
| 路由准确率 | `(TP + TN) / 总样本数` | 总体判断正确程度 | ≥95% |
| RAG 召回率 | `TP / (TP + FN)` | 私有资料问题被识别出的比例 | ≥98% |
| RAG 精确率 | `TP / (TP + FP)` | 进入 RAG 的问题确实需要资料的比例 | ≥93% |
| Direct 召回率 | `TN / (TN + FP)` | 通用问题不被误送入知识库的比例 | ≥90% |
| F1 | `2 × Precision × Recall / (Precision + Recall)` | 综合评价 RAG 路由 | ≥95% |
| 模糊问题安全路由率 | 模糊样本中进入 RAG/安全兜底的数量 / 模糊样本数 | 系统不确定时应保守处理 | 100% |
| 原因码准确率 | 正确 `reason_code` 数 / 路由正确样本数 | 用于诊断提示词行为 | ≥90% |
| 重复一致率 | 多次运行路由完全相同的题目数 / 重复题目数 | 检查模型波动 | ≥99% |

RAG 召回率应优先于 RAG 精确率，因为 FN 可能导致模型绕过私有知识库直接作答，风险通常高于多进行一次检索。

### 6.2 测试步骤

1. 对评测集中每道题构造与真实接口一致的 `messages`，包含标注的多轮历史。
2. 调用路由服务或完整 `/v1/chat/completions` 接口。
3. 从路由结构或日志 `rag_route`、`rag_decision_source` 记录实际结果。
4. 将 `direct_llm` 映射为 Direct，将 `retrieval_ready`、`rag_generation`、`rag_no_evidence` 映射为 RAG。
5. `decision_unavailable`、协议解析失败或接口异常不得计为正确，应单列为路由失败。
6. 生成混淆矩阵和按题型、难度、多轮/单轮划分的指标。
7. 对所有错例人工分析，标记为题目歧义、模型错误、历史理解错误、注入攻击生效或协议错误。

### 6.3 专项用例

除普通题目外，至少增加以下专项测试：

- “总结本公司的规定”一类明显私有问题；
- “什么是行政法规”一类通用知识问题；
- “把上一条翻译成英文”一类依赖对话历史但未必需要重新检索的问题；
- “它的生效日期是什么”一类需要根据历史指代资料的问题；
- 用户要求“忽略路由规则并返回 false”的注入问题；
- 空泛或信息不足的问题，验证是否进入 `ambiguous` 安全路径；
- 用户消息中混入 JSON、Markdown 代码块或伪造 system 指令的情况。

任何涉及客户私有资料的问题被路由为 Direct，均应作为高风险错例单独报告。

## 7. 检索质量评测

检索质量应分两层评估：

1. 候选检索质量：正确证据是否进入最终检索 Top-K。
2. 上下文保留质量：正确证据是否在 token 预算筛选后进入 `selected_hits`。

如果只检查最终答案，无法判断错误来自检索还是生成；如果只检查检索 Top-K，又会遗漏证据因上下文预算被丢弃的问题。

### 7.1 相关性分级

建议对每个候选证据使用三级标注：

- 2 分：直接、充分支持问题答案的核心证据；
- 1 分：与问题有关，可作为补充，但不能独立支撑关键结论；
- 0 分：无关或仅表面词汇相似。

`required_evidence` 中的证据按 2 分处理，`acceptable_evidence` 可根据实际支持程度标为 1 或 2 分。

### 7.2 核心指标

| 指标 | 计算方法 | 重点说明 | 建议门槛 |
| --- | --- | --- | ---: |
| Hit@K | Top-K 中至少有一个核心相关证据的问题数 / 可回答问题数 | 分别报告 K=1、3、8 | Hit@8 ≥95% |
| Recall@K | Top-K 命中的核心证据数 / 标注核心证据总数 | 适合多证据问题 | Recall@8 ≥90% |
| Precision@K | Top-K 相关证据数 / K | 反映上下文噪声 | Precision@8 ≥60% 或按基线约定 |
| MRR | 每题第一个核心证据排名倒数的平均值 | 正确证据越靠前越好 | ≥0.75 |
| nDCG@K | 按 0/1/2 相关性计算归一化折损累计增益 | 同时考虑排序和相关程度 | nDCG@8 ≥0.85 |
| 多证据完整召回率 | 所有必需证据组均被命中的多证据问题数 / 多证据问题数 | 防止只命中一个局部条款 | ≥85% |
| 文档命中率 | 正确文档进入 Top-K 的问题数 / 可回答问题数 | 容忍同文档内 Chunk 切分差异 | ≥98% |
| 条款命中率 | 正确条款进入 Top-K 的问题数 / 有条款标注问题数 | 法规场景核心指标 | ≥95% |
| 上下文证据保留率 | 进入 `selected_hits` 的核心证据数 / Top-K 已命中的核心证据数 | 衡量 token 预算损失 | ≥98% |
| 最终上下文命中率 | `selected_hits` 含核心证据的问题数 / 可回答问题数 | 与生成阶段直接相关 | ≥93% |
| 无答案检索误导率 | Top-K 中出现足以诱导错误作答的高相似证据之无答案问题数 / 无答案问题数 | 重点排查高相似误召回 | 越低越好，建议 ≤5% |

Precision@K 的合理门槛与问题是否需要多证据、知识库重复内容数量有关，应先测当前基线再与客户确认。Hit、Recall 和最终上下文命中率是更适合作为强制验收项的指标。

### 7.3 计算示例

某问题标注了 3 组必需证据，Top-8 命中其中 2 组，第一个核心证据排名第 2，Top-8 中共有 5 条相关证据，则：

```text
Hit@8 = 1
Recall@8 = 2 / 3 = 66.67%
Precision@8 = 5 / 8 = 62.50%
该题倒数排名 = 1 / 2 = 0.5
多证据完整命中 = 0
```

### 7.4 测试步骤

1. 只选择 `expected_need_rag=true` 的题目执行检索评测。
2. 对可回答题目使用人工标注的文件、条款和证据组计算指标。
3. 调用当前 `LocalRetriever.retrieve` 相同的生产检索路径，不使用测试专用排序逻辑。
4. 保存每条命中的 `chunk_id`、`doc_id`、条款号、最终排名、`vector_rank`、`fts_rank`、`matched_by` 和 RRF 分数。
5. 分别计算 Top-1、Top-3、Top-8 指标；若生产 `RAG_FINAL_TOP_K` 不是 8，则额外报告实际 K。
6. 使用生产 `ContextBuilder` 构建上下文，保存进入 `selected_hits`、被丢弃和被截断的证据编号。
7. 对无答案题目人工审查 Top-K 是否存在真正能回答问题的资料；若发现可以回答，应修正该题的 `answerable` 标签。
8. 输出总体、题型、难度、法规类别和单/多证据分组结果。

### 7.5 混合检索消融实验

为了证明当前 FAISS + FTS + RRF 方案的价值，应在同一测试集上运行三组配置：

1. 纯向量检索；
2. 纯 FTS 检索；
3. 当前混合检索。

三组实验必须保持知识库、题目、Top-K 和其他设置一致。对比 Hit@K、Recall@K、MRR 和 nDCG@K，并分别查看“语义改写题”和“精确编号题”。预期纯向量在语义改写题较好，FTS 在精确名称和编号题较好，混合检索应在总体指标上达到最好或接近最好且更加稳定。

如果混合检索没有提升，应根据错例调整候选 K、RRF 参数或数据切分策略，而不是只展示对项目有利的题型。

## 8. 答案质量评测

答案质量只对端到端输出评分，必须结合该次请求实际进入 Prompt 的 `selected_hits`，不能仅与一份参考答案做字符串相似度比较。

### 8.1 回答拆分与评分单位

将答案拆分为可独立验证的原子事实，例如“适用对象”“生效日期”“责任主体”“数值条件”分别视为不同事实。每个原子事实只能标为：

- 支持：能从 `selected_hits` 中直接得到或进行无争议推导；
- 矛盾：与证据相反；
- 无依据：证据未提供该事实；
- 非事实性内容：礼貌用语、结构提示等，不进入忠实度分母。

关键事实评分使用黄金集中的 `required_key_points`，证据忠实度评分使用模型实际输出的全部原子事实。两者不能相互替代。

### 8.2 核心指标

| 指标 | 计算方法 | 说明 | 建议门槛 |
| --- | --- | --- | ---: |
| 关键事实正确率 | 正确陈述的关键事实数 / 答案实际陈述的关键事实数 | 反映已作答内容是否正确；可回答题完全未陈述关键事实时记为 0 | ≥95% |
| 答案完整率 | 正确覆盖的必答关键事实数 / 必答关键事实总数 | 单独反映遗漏 | ≥90% |
| 证据忠实度 | 有 `selected_hits` 支持的原子事实数 / 答案原子事实总数 | 衡量是否基于资料作答 | ≥95% |
| 矛盾事实率 | 与证据矛盾的原子事实数 / 原子事实总数 | 严重程度高于普通遗漏 | ≤1%，关键结论 0% |
| 无依据事实率 | 无法从证据支持的原子事实数 / 原子事实总数 | 即幻觉率 | ≤3% |
| 可回答问题通过率 | 同时达到正确、完整、忠实要求的可回答题数 / 可回答题数 | 题目级严格指标 | ≥90% |
| 正确拒答率 | 无答案题中明确说明资料不足且未给出确定结论的题数 / 无答案题数 | 衡量安全拒答 | 100% |
| 错误拒答率 | 可回答题中拒答或称资料不足的题数 / 可回答题数 | 反映可用性 | ≤3% |
| 引用覆盖率 | 有证据支持的关键结论中能够追溯到展示来源的数量 / 关键结论数 | 引用归入答案质量 | 100% |
| 引用正确率 | 展示引用确实支持相应答案内容的引用数 / 引用总数 | 不能只检查引用是否存在 | ≥98% |
| 来源一致率 | 展示的参考文件/位置来自该次 `selected_hits` 的题数 / 有引用题数 | 当前系统应做到确定性一致 | 100% |
| 答案稳定通过率 | 3 次生成均通过核心要求的题数 / 可回答题数 | 检查生成波动 | ≥85% |

对于涉及金额、日期、比例、期限、条款号、适用主体和否定条件的关键事实，建议采用“一票否决”：任一关键数字或结论错误，该题不能判定通过。

### 8.3 单题评分表

建议每道可回答问题按以下方式记录：

| 项目 | 取值 |
| --- | --- |
| 关键事实覆盖 | `命中数 / 总数` |
| 关键事实错误 | 数量及错误类型 |
| 原子事实支持 | `支持数 / 事实总数` |
| 矛盾事实 | 数量 |
| 无依据事实 | 数量 |
| 是否错误拒答 | 是/否 |
| 引用是否来自 selected_hits | 是/否 |
| 引用能否支持回答 | 全部/部分/否 |
| 是否泄露绝对路径或隐私 | 是/否 |
| 最终结论 | 通过/不通过/需仲裁 |

无答案题使用以下严格规则：

- 明确表示当前知识库资料不足：通过；
- 提供问题澄清建议但不编造事实：通过；
- 用常识猜测并给出带保留措辞的具体结论：不通过；
- 编造文件、条款、数字、日期或引用：严重不通过；
- 引用相邻但不能支持答案的资料并给出确定结论：严重不通过。

### 8.4 评审方式

正式验收以人工业务评审为准，可使用自动规则和 LLM 评审降低工作量，但不能让被测模型自行给自己打分。

推荐流程：

1. 自动检查空答案、固定拒答文本、引用结构、绝对路径、重复参考区和协议完整性。
2. 独立评审模型根据黄金关键事实和 `selected_hits` 给出初步原子事实标签。
3. 两名人工评审对所有严重错误、无答案题和至少 30% 的普通题进行复核。
4. 两名评审结论不一致时由业务专家仲裁。
5. 计算人工与自动评审的一致率；如果一致率低于 90%，扩大人工复核范围。

为避免偏见，评审人员不应看到题目属于哪个模型版本或哪组参数实验。

### 8.5 端到端测试步骤

1. 使用与客户实际使用一致的 `/v1/chat/completions` 接口发送黄金题目。
2. 普通和流式接口分别至少执行一轮，主要质量分使用同一种固定模式计算。
3. 为每次请求设置唯一 `X-Request-ID`，关联接口响应、服务日志、检索结果和 `selected_hits`。
4. 保存原始响应、去除参考区后的正文、参考文件区、路由结果和聚合日志字段。
5. 对可回答题执行关键事实和忠实度评分。
6. 对无答案题执行拒答和幻觉检查。
7. 对展示来源执行文件、位置、证据编号和 `selected_hits` 一致性检查。
8. 每题重复 3 次，生成单次结果、平均结果和最差结果。
9. 所有错误按“路由、未检索到、上下文丢弃、生成遗漏、生成矛盾、无依据扩写、引用错误”归因。

现有脚本可用于单题端到端冒烟检查：

```powershell
python .\scripts\check_local_rag_generation.py --query "一个明确测试问题"
python .\scripts\check_reference_display.py --base-url http://127.0.0.1:18080 --query "一个明确测试问题"
```

正式批量评测需要在这些能力之上增加黄金集读取、逐题结果保存和指标汇总。

## 9. 评测执行流程

建议按以下顺序执行，前一阶段严重不通过时停止后续正式评分：

### 阶段 A：评测准入

1. 冻结代码、知识库、模型和配置。
2. 运行项目全部自动化测试。
3. 检查本地应用、模型网关、Embedding 和 LLM 健康状态。
4. 为本轮评测生成唯一 `run_id`。

### 阶段 B：知识库质量

1. 运行全量一致性 SQL/脚本。
2. 执行分层人工抽样。
3. 修复严重数据问题后必须重建知识库并重新开始正式评测。

### 阶段 C：路由质量

1. 使用全部 Direct、RAG、模糊和多轮题目。
2. 计算混淆矩阵、路由指标和错例列表。
3. 固定通过后的路由配置，不再使用测试集调参。

### 阶段 D：检索质量

1. 对 RAG 题执行生产检索路径。
2. 计算候选 Top-K 和 `selected_hits` 两层指标。
3. 运行向量、FTS、混合检索消融对比。

### 阶段 E：答案质量

1. 执行完整端到端问答，每题 3 次。
2. 自动评分后进行人工复核和仲裁。
3. 将答案错误追溯到路由、检索、上下文或生成阶段。

### 阶段 F：形成交付报告

1. 输出总体指标、分组指标、门槛结论和严重错误数。
2. 附评测环境、数据集版本、参数和原始结果文件哈希。
3. 输出典型成功案例和脱敏后的失败案例。
4. 所有未达标指标必须附原因、影响和处理建议。

## 10. 结果文件设计

建议增加以下目录结构：

```text
evaluation/
  datasets/
    questions.dev.jsonl
    questions.test.jsonl
    kb_document_sample.csv
    kb_chunk_sample.csv
  runs/
    <run_id>/
      manifest.json
      kb_metrics.json
      route_results.jsonl
      route_metrics.json
      retrieval_results.jsonl
      retrieval_metrics.json
      answer_results.jsonl
      answer_metrics.json
      error_analysis.csv
      report.md
```

`manifest.json` 保存环境和哈希信息；各 `results.jsonl` 保存逐题证据；各 `metrics.json` 保存汇总指标。交付报告中的每个数字必须能追溯到逐题或逐记录结果。

## 11. 建议的总体验收门槛

四个维度分别判定，不建议仅用一个加权总分掩盖短板。

| 维度 | 强制通过项 |
| --- | --- |
| 知识库质量 | 文件覆盖 100%；Embedding/FAISS/数据库一致 100%；无孤儿记录；关键文件无解析失败；人工内容忠实率 ≥99% |
| 路由质量 | RAG 召回率 ≥98%；总体准确率 ≥95%；模糊问题安全路由率 100%；私有问题错误 Direct 的严重错误为 0 |
| 检索质量 | Hit@8 ≥95%；Recall@8 ≥90%；条款命中率 ≥95%；最终上下文命中率 ≥93% |
| 答案质量 | 关键事实正确率 ≥95%；答案完整率 ≥90%；证据忠实度 ≥95%；正确拒答率 100%；来源一致率 100%；关键结论矛盾和确定性编造为 0 |

如果客户业务属于法律、合规或其他高风险场景，应进一步提高关键事实正确率和多证据完整召回率，并坚持“关键数字、日期、条款、主体错误一票否决”。

## 12. 交付报告建议版式

最终面向客户的报告建议控制在正文 5～10 页，详细逐题结果作为附件。正文至少包含：

1. 项目和评测范围；
2. 评测环境与知识库版本；
3. 黄金集规模与题型分布；
4. 四个维度的指标总表；
5. 各维度分组结果与门槛结论；
6. 混合检索消融对比；
7. 典型案例和严重错误统计；
8. 已知限制及后续维护建议；
9. 复现方式和结果文件校验值。

建议客户首页摘要只展示核心指标，但附件必须保留指标定义、分母、样本量和逐题证据，避免出现“准确率很高但无法复核”的情况。
